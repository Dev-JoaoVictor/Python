distancia = float(input('Digite a distância entre duas cidades: '))
tempo = float(input('Digite o tempo gasto entre duas cidades: '))
velocidadeMedia = print(f'A velocidade média é de: {(distancia/tempo):.0f} km/h')
